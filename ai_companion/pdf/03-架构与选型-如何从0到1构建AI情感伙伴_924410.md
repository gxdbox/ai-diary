# 03｜架构与选型：如何从0到1构建AI情感伙伴？

> 无

> 今天我将带你完成一次完整的端到端实践之旅——从一个模糊的创意“我想做一个能倾听、理解并回应情绪的AI伙伴”，到最终构建出一个具备上下文感知、人格化表达、长期记忆与安全机制的可运行原型系统。

<audio controls src="audio/03-架构与选型-如_924410_dc78722402.mp3"></audio>

你好，我是袁从德。

在前两讲中，我们完成了从宏观认知到场景聚焦的思维跃迁。今天我将带你完成一次完整的端到端实践之旅——从一个模糊的创意“我想做一个能倾听、理解并回应情绪的AI伙伴”，到最终构建出一个具备上下文感知、人格化表达、长期记忆与安全机制的可运行原型系统。这不仅是一次技术实现，更是一场融合用户需求洞察、系统架构设计与AI能力调用的综合实践。

我们将遵循第一讲提出的“开发路径五步法”：需求定义 → 技术选型 → 开发集成 → 评估优化 → 部署运维，围绕“心理健康陪伴机器人”这一具体案例，深入拆解每一个环节的关键决策点和实现细节。

你不需要是算法专家，也不必掌握复杂的深度学习知识。我们将基于主流的大模型平台（如OpenAI、通义千问等）、低代码工具链和开源框架（如LangChain、LlamaIndex），用工程化的方式，把大模型的能力“组装”成真正可用的产品模块。

通过这一讲的学习，你将获得：

一套可复用的情感聊天机器人需求分析框架。

一份清晰的技术架构图与组件选型建议。

多个关键功能模块的实现代码示例（含提示工程模板、向量数据库接入、记忆管理逻辑）。

对AI产品“可控性”与“人性化”之间平衡的理解。

以及最重要的——亲手构建第一个大模型应用的信心与能力。

让我们开始这场从0到1的构建之旅。

一、需求分析：定义你的AI“情感伙伴”

任何成功的应用，都始于对“用户是谁”和“解决什么问题”的深刻理解。对于情感聊天机器人而言，尤其如此——它处理的是最敏感、最复杂的人类心理活动。

1.1 明确目标用户与核心场景

我们以“心理健康陪伴机器人”为例，先回答三个基本问题。

谁会使用它？可能包括：感到孤独的年轻人、压力过大的职场人、缺乏倾诉渠道的学生、不愿面对传统心理咨询的个体、或需要日常情绪支持的慢性病患者。

他们在什么情境下使用？深夜失眠时的情绪宣泄、工作受挫后的自我怀疑、人际关系冲突后的迷茫、或仅仅是想要一个“不会评判我”的倾听者。

他们真正需要的是什么？不是诊断，也不是治疗，而是被听见、被理解、被共情。心理学研究表明，有效的倾听本身就具有疗愈作用（Rogers, 1957）。我们的目标不是替代心理咨询师，而是提供一条低门槛、即时响应的“情感缓冲带”。

产品定位
一款基于大模型的非医疗级心理健康支持助手，旨在通过自然语言对话，为用户提供情绪接纳、认知疏导与正向反馈，帮助其缓解短期压力、提升自我觉察。

1.2 功能需求分层设计

将抽象需求转化为具体功能，建议采用“核心功能 + 增强功能 + 安全边界”的三层结构，如表3-1所示。
<img src="image/03-架构与选型-如_924410_019c5a9e1f.jpg" alt="" title="表 3-1 功能需求" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

MVP（最小可行产品）建议：
初期聚焦核心功能+安全边界，快速验证对话质量与用户接受度，后续迭代增强功能。

1.3 非功能性需求：不可忽视的“软指标”

除了“能做什么”，我们还要考虑“做得好不好”。以下是一些软指标。

响应速度：理想延迟 < 2秒，避免打断情绪流动。

人格一致性：AI应有稳定的声音风格（如温暖、耐心、中立）。

隐私保护：对话数据加密存储，用户可随时删除历史记录。

可解释性：让用户知道“AI不是真人”，避免情感依赖错位。

二、技术选型：搭建系统的“骨架”

明确了“做什么”，接下来要决定“怎么造”。情感聊天机器人的技术栈虽轻量，但涉及多个关键组件的协同。

我们采用分层架构设计，便于模块化开发与后期扩展，如图3-1所示。情感聊天机器人的分层架构设计图，共包含访问层、对话管理层、核心引擎层、知识与记忆层、数据与服务层五层，各层分别对应用户界面（Web/App/ 小程序）、会话状态与上下文维护、大模型 API + 提示工程 + Agent 逻辑、向量数据库（长期记忆）与规则库（安全过滤）、用户数据 / 日志与第三方服务（如 TTS / 语音）等功能，以分层方式实现模块化开发与后期扩展。

<img src="image/03-架构与选型-如_924410_c0408b06d6.jpg" alt="" title="图 3-1 系统骨架" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

2.1 大模型选择：闭源 vs 开源

我们经常听到这样的说法：“开源LLM是免费的。”

这个观点从技术、经济、战略层面来看都是错误的。如果你正在构建任何严肃的产品，这种想法甚至可能是危险的天真。开源LLM并非真正免费——它们只是将成本从许可转移到了工程、基础设施、维护和战略风险上。

<img src="image/03-架构与选型-如_924410_572d2dbbaf.jpg" alt="" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

<img src="image/03-架构与选型-如_924410_8a06f56e0d.png" alt="图片" title="图 3-2 大模型尺寸和成本的关系（岛上的狮子《云端 or 本地，闭源 or 开源？深入了解企业部署开源 LLM 模型的真实成本》" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

如表3-2所示，闭源模型（以 GPT-4o、通义千问 Max 为代表）语言能力强、稳定性高且生态完善，但成本较高、存在数据出境风险，适合快速验证与商业产品场景；开源模型（如 Llama 3、ChatGLM3、Qwen-7B）具备数据可控、可本地部署、成本低的优势，不过需要调优且对推理资源要求高，更适用于敏感场景与私有化部署。
<img src="image/03-架构与选型-如_924410_be8a69b0c8.jpg" alt="" title="表 3-2 闭源模型和开源模型对比" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

建议：初期使用通义千问或GPT-4o API进行快速原型开发；成熟后可考虑私有化部署开源模型。

2.2 开发框架：LangChain vs LlamaIndex vs 自研

在大模型应用开发中，LangChain、LlamaIndex 与自研框架的选择需结合场景复杂度、开发效率与功能需求综合考量：

LangChain：适合构建复杂Agent流程，支持多种模型、工具集成，社区活跃。

LlamaIndex：专精于RAG与结构化数据检索，适合知识增强型应用。

自研轻量框架：适合简单对话系统，控制更精细，但开发成本高。

推荐组合：使用 LangChain + 向量数据库（如Chroma/Pinecone） 构建基础对话流，后期按需引入LlamaIndex做知识增强。

2.3 记忆存储：短期 vs 长期

短期记忆（会话内）：使用 ConversationBufferMemory 或 SummaryMemory，将最近N轮对话拼接为上下文传给大模型。

长期记忆（跨会话）：将用户的关键陈述（如“我最近总是失眠”“我喜欢蓝色”）提取为记忆片段，存入向量数据库，通过语义搜索实现“回忆”。

示例：当用户说“我又做噩梦了”，系统可检索过往“睡眠问题”相关记忆，回应：“你之前提到过几次噩梦，最近是不是压力又变大了？”

2.4 安全过滤：必须的“防火墙”

建议采用“双层过滤”机制：

输入层过滤：使用规则引擎或轻量模型检测敏感词、攻击性语言；

输出层监控：对AI生成内容做合规性检查，防止生成有害建议。

可集成开源方案如ModerateContent或阿里云内容安全API。

三、开发集成：一步步构建你的AI伙伴

现在进入动手环节。我们将用 Python + LangChain + Qwen API 演示核心功能实现。（图3-3）

完整代码可以从这个链接获取：https://github.com/congde/emotional_chat.git

<img src="image/03-架构与选型-如_924410_84e5566ac7.png" alt="图片" title="图 3-3 聊天机器人" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

3.1 项目代码结构

这是一个前后端分离的情感聊天系统。后端采用 FastAPI 框架，集成 SQLAlchemy + MySQL 数据库、LangChain 框架和 Qwen API，实现了带情感分析的智能对话功能，并配备向量数据库支持语义检索；前端使用 React 构建单页应用，通过 API 服务层与后端通信，提供现代化的聊天交互界面。

项目的核心功能由三大模块组成：backend/modules/llm/core/llm_core.py 中的 SimpleEmotionalChatEngine 类负责主聊天逻辑和 Qwen 模型调用，backend/emotion_analyzer.py 提供情感识别能力，backend/vector_store.py 实现对话历史的向量化存储与检索。

项目提供了灵活的部署方案，包括主入口 run_backend.py（启动 FastAPI）、备用方案 simple_backend.py（Flask）以及一键启动脚本 start.sh，配置通过 config.py 和 config.env 统一管理。

```bash
emotional_chat/
├── backend/                          # 后端代码目录
│   ├── __init__.py                  # 包初始化文件
│   ├── app.py                       # ✅ FastAPI应用工厂（主入口）
│   ├── main.py                      # FastAPI主应用程序（兼容性保留）
│   ├── database.py                  # 数据库模型和操作（SQLAlchemy + MySQL）
│   ├── models.py                    # Pydantic数据模型定义
│   ├── emotion_analyzer.py          # ✅ 情感分析器（基于关键词和LLM）
│   ├── vector_store.py              # ✅ 向量数据库操作（Chroma）
│   ├── xinyu_prompt.py              # 心语Prompt配置和安全过滤
│   │
│   ├── routers/                     # 路由层 - API接口定义
│   │   ├── chat.py                  # 聊天接口
│   │   ├── memory.py                # 记忆管理接口
│   │   ├── feedback.py              # 反馈接口
│   │   └── evaluation.py            # 评估接口
│   │
│   ├── services/                    # 服务层 - 业务逻辑
│   │   ├── chat_service.py          # 聊天服务
│   │   ├── memory_service.py        # 记忆服务
│   │   └── context_service.py       # 上下文服务
│   │
│   └── modules/                     # 模块层 - 核心功能
│       ├── llm/                     # LLM模型封装
│       │   └── core/
│       │       └── llm_core.py      # ✅ 主聊天引擎（SimpleEmotionalChatEngine类）
│       │                            #     - Qwen API + LangChain + 情感分析
│       │                            #     - LCEL表达式链构建
│       │                            #     - 短期记忆（MySQL）+ 长期记忆（向量数据库）
│       │
│       ├── agent/                   # Agent智能核心（可选）
│       ├── rag/                     # RAG知识库系统（可选）
│       └── intent/                  # 意图识别模块（可选）
│
├── frontend/                         # 前端代码目录
│   ├── public/                      # 静态资源目录
│   │   └── index.html               # HTML入口模板
│   ├── src/                         # React源代码
│   │   ├── App.js                   # 主应用组件（聊天界面）
│   │   ├── index.js                 # React入口文件
│   │   ├── index.css                # 全局样式文件
│   │   └── services/                # 服务层
│   │       └── ChatAPI.js           # 后端API调用封装
│   ├── package.json                 # 项目配置和依赖声明
│   ├── package-lock.json            # 依赖锁定文件
│   └── node_modules/                # 前端依赖包（自动生成）
│
├── config.py                         # 全局配置文件（端口、路径等）
├── config.env                        # 环境变量配置（Qwen API Key、数据库等）
├── env_example.txt                   # 环境变量配置模板
├── setup_database.py                 # 数据库初始化脚本
├── run_backend.py                    # 后端启动脚本（主入口）
├── simple_backend.py                 # 备用后端启动脚本（Flask）
├── start.sh                          # 一键启动脚本（前后端同时启动）
├── requirements.txt                  # Python依赖列表
├── LICENSE                           # MIT开源许可证
└── README.md                         # 项目文档（本文件）
```

核心文件说明你可以参考后面的表格。

<img src="image/03-架构与选型-如_924410_14e6ef464e.jpg" alt="" title="表 3-3 核心文件说明" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

3.2 基础对话链：让AI“会聊天”

在 LangChain 中，LCEL（LangChain Expression Language）是构建处理流程的核心方式，它通过直观的“管道”（| 操作符）将不同组件串联起来，形成可复用、可扩展的处理链。chain = prompt | model | output_parser 正是 LCEL 最经典的基础链结构，其核心是实现“输入处理→模型调用→输出解析”的完整流程。

组件化与管道流 LCEL 的设计理念是“组件化”—— 将大语言模型交互的各个环节拆分为独立组件（如提示词构建、模型调用、输出解析等），再通过 | 符号（类似 Unix 管道）将这些组件按顺序连接，形成数据处理的“流水线”。

在 prompt → model → output_parser 这个链中，数据会按顺序流过每个组件：原始输入先进入 prompt 组件，被格式化为模型可理解的提示词；格式化后的提示词再流入 model 组件，由大语言模型生成原始输出；模型的原始输出最后进入 output_parser 组件，被解析为更易用的格式（如字符串、字典等）。 这种流程就像工厂的生产线，每个环节专注于自己的任务，最终输出成品。

各组件的角色与作用：

prompt：负责将原始输入（如用户问题、上下文信息等）格式化为符合模型要求的提示词结构。

model：大语言模型（核心推理） model 是链的核心，负责接收 prompt 生成的完整提示词，进行推理并生成原始输出。

output_parser：输出解析器（结果格式化） output_parser 负责将模型的原始输出转换为更易用的格式。

当调用 chain.invoke(inputs) 时，整个流程会自动触发 python 运行。假设输入为用户问题，inputs = {“user_input”: “我最近总是失眠，该怎么调整？”}。

执行步骤为：

prompt 接收 inputs，生成完整提示词，再传给 model。

model 接收提示词，调用大模型生成原始输出，随后传给 output_parser。

output_parser 接收原始输出，解析为字符串，再返回最终结果。

实际代码位置backend/modules/llm/core/llm_core.py 中的 SimpleEmotionalChatEngine.__init__() 方法。

```python
# backend/modules/llm/core/llm_core.py

class SimpleEmotionalChatEngine:
    def __init__(self):
        # ... 初始化配置 ...

        # 初始化 LangChain 组件（LCEL 表达式）- 如果可用
        if self.api_key and LANGCHAIN_AVAILABLE:
            try:
                # 1. 初始化模型
                # 通过 LangChain 的 ChatOpenAI 组件，利用 DashScope 提供的 OpenAI 兼容接口调用通义千问。
                self.llm = ChatOpenAI(
                    model=self.model,
                    temperature=0.7,
                    api_key=self.api_key,
                    base_url=self.api_base_url
                )

                # 2. 定义 AI 人格与行为准则（提示模板）
                self.template = """{system_prompt}
                              {{long_term_memory}}
                               对话历史：{{history}}
                               用户：{{input}}
                               心语：""".format(system_prompt=XINYU_SYSTEM_PROMPT)

                # 3. 创建提示模板和链（LCEL表达式）
                self.prompt = ChatPromptTemplate.from_template(self.template)
                self.output_parser = StrOutputParser()
                # 构建链：chain = prompt | model | output_parser
                self.chain = self.prompt | self.llm | self.output_parser
                print("✓ LangChain LCEL 链初始化成功")
            except Exception as e:
                print("警告: LangChain 初始化失败，将使用传统方式: {}".format(e))
                self.llm = None
                self.chain = None
```

这种方式的优势在于：

灵活性：每个组件可独立替换（例如换用GPT模型，只需替换 model 组件，无需修改 prompt 和 parser）；

可扩展性：可在链中插入新组件（如加入 memory 组件实现对话历史记忆，形成 prompt → memory → model → parser）；

可读性：通过 | 符号直观展示流程，比传统函数嵌套更易理解。

3.3 添加上下文记忆

还记得电影《她》里，AI 助手萨曼莎最动人的瞬间吗？不是精准的信息应答，而是她会记得主角上周提过的“童年常去的梧桐巷”，会在他情绪低落时接住上次没说完的“工作烦恼”，甚至能顺着他曾喜欢的爵士乐话题，聊到新发现的小众唱片。这种“记得”，让冰冷的算法有了情感的褶皱，也让情感聊天跳出了 一问一答”的机械循环。​

我们在项目中加入上下文记忆，实际代码位置 backend/modules/llm/core/llm_core.py 中的 get_openai_response() 方法。

```python
# backend/modules/llm/core/llm_core.py

def get_openai_response(self, user_input, user_id, session_id):
    """使用 LangChain LCEL 链生成回应（如果可用），否则使用传统HTTP请求"""
    # 安全检查
    is_safe, warning = self.is_safe_input(user_input)
    if not is_safe:
        return warning

    # 构建历史对话（短期记忆 - MySQL）
    db_manager = DatabaseManager()
    with db_manager as db:
        recent_messages = db.get_session_messages(session_id, limit=10)
        history_text = ""
        for msg in reversed(recent_messages[-5:]):  # 最近5条消息
            history_text += "{}: {}\n".format('用户' if msg.role == 'user' else '心语', msg.content)

    # 从向量数据库检索相似对话（长期记忆）
    long_term_context = ""
    if self.vector_store:
        try:
            # 检索相似的历史对话（跨会话）
            similar_conversations = self.vector_store.search_similar_conversations(
                query=user_input,
                session_id=None,  # 不限制会话，检索所有历史
                n_results=3
            )

            if similar_conversations and similar_conversations['documents']:
                long_term_context = "\n相关历史对话参考：\n"
                for doc in similar_conversations['documents'][0][:2]:  # 取前2个最相似的
                    long_term_context += "- {}\n".format(doc[:100])  # 限制长度
                long_term_context += "\n"
        except Exception as e:
            print("向量检索失败: {}".format(e))

    # 优先使用 LCEL 链（如果可用）
    if self.chain:
        try:
            # 使用链生成回应 (chain.invoke) - 包含长期记忆
            response = self.chain.invoke({
                "long_term_memory": long_term_context,
                "history": history_text.strip(),
                "input": user_input
            })
            return response
        except Exception as e:
            print("LangChain调用失败 ({}): {}，尝试传统方式".format(self.model, e))
            # 继续使用传统方式

    # 使用传统 HTTP 请求方式（兼容模式）
    return self._call_api_traditional(user_input, history_text, long_term_context)
```

上下文记忆，正是情感聊天的灵魂支点：它让 AI 不再是每次对话都要重新“认识”你的工具 —— 不用反复解释“我喜欢猫”“怕黑”，不用在聊到一半时重复前情；它能接住你偶然流露的细碎情绪，比如上次吐槽的通勤堵车，下次聊出行时会自然避开相关话题；更能让情感在“延续性” 里慢慢沉淀：从“记得你生日快到了”到“想起你说过想尝试烘焙”，这种带着过往痕迹的回应，才是情感聊天该有的温度 —— 不是生硬的共情话术，而是我真的记得你的踏实感。​

说到底，情感聊天的核心需求从不是答对问题，而是被理解、被重视。而上下文记忆，正是把这种重视落到实处的关键：它让 AI 的回应带着你们专属的“对话记忆”，让每一次聊天都像和熟悉的人续谈，而非与陌生的机器初遇。这，才是情感聊天真正的价值所在。

3.4 实现长期记忆（向量数据库）

对话系统采用双层记忆架构：MySQL 负责短期记忆（当前会话的最近5-10条消息），ChromaDB 向量数据库负责长期记忆（跨会话的语义相似对话检索）。

在 SimpleEmotionalChatEngine 的 chat() 方法中，完整的处理流程如下。

用户输入处理：接收用户消息 → 情感分析 → 保存用户消息到 MySQL

生成回复前的记忆检索：在 get_openai_response() 方法中，先从 MySQL 获取最近5条消息作为短期上下文，再从向量数据库检索语义相似的历史对话作为长期上下文

回复生成：将短期记忆和长期记忆同时传入 prompt 模板，调用通义千问 API 生成回复

回复后的记忆保存：保存助手回复到 MySQL，并将完整的对话对（用户消息+助手回复）保存到向量数据库，供未来检索使用 这种设计实现了"检索在生成前，保存在生成后"的流程，确保每次生成回复时都能利用历史相似对话的上下文，同时将新对话持续积累到长期记忆中。

为实现长期记忆功能，需要在 backend/modules/llm/core/llm_core.py 中的 SimpleEmotionalChatEngine 类中完成以下三项核心操作。

第一步：初始化向量数据库（在 __init__ 方法中）

导入 VectorStore 类并创建实例，存储在 self.vector_store 属性中。

如果初始化失败，系统会降级为仅使用 MySQL 短期记忆。

第二步：生成回复前检索相似对话（在 get_openai_response() 方法中）

使用 self.vector_store.search_similar_conversations() 方法，以当前用户输入为查询，检索最相似的3条历史对话。

将检索结果格式化为 long_term_context 字符串，传入 prompt 模板的 {{long_term_memory}} 占位符。

检索时不限制 session_id，实现跨会话的语义检索。

第三步：生成回复后保存对话（在 chat() 方法中）

在生成并保存助手回复到 MySQL 后，调用 self.vector_store.add_conversation() 方法。

将完整的对话对（用户消息+助手回复+情感标签）存储到向量数据库。

存储格式为：“用户: {message}\n助手: {response}\n情感: {emotion}”。

实际代码位置：

向量数据库类: backend/vector_store.py 中的 VectorStore 类

集成调用: backend/modules/llm/core/llm_core.py 中的 SimpleEmotionalChatEngine 类

1.初始化向量数据库（在**__init__**方法中）

```python
# backend/modules/llm/core/llm_core.py
# 尝试导入向量数据库
try:
   from backend.vector_store import VectorStore
   VECTOR_STORE_AVAILABLE = True
except ImportError as e:
   VECTOR_STORE_AVAILABLE = False
   print("提示: 向量数据库模块未安装 ({}), 将仅使用MySQL短期记忆".format(e))
# 初始化向量数据库（长期记忆）
if VECTOR_STORE_AVAILABLE:
   try:
       self.vector_store = VectorStore()
       print("✓ 向量数据库 (Chroma) 初始化成功")
   except Exception as e:
       print("警告: 向量数据库初始化失败: {}，将仅使用MySQL".format(e))
       self.vector_store = None
else:
   self.vector_store = None
   print("⚠ 向量数据库未安装，仅使用MySQL短期记忆")
```

2. 生成回复前检索相似对话（在 get_openai_response() 方法中）

```python
# backend/modules/llm/core/llm_core.py
def get_openai_response(self, user_input, user_id, session_id):
   # ... 安全检查、MySQL短期记忆获取 ...

   # 从向量数据库检索相似对话（长期记忆）
   long_term_context = ""
   if self.vector_store:
       try:
           # 检索相似的历史对话（跨会话）
           similar_conversations = self.vector_store.search_similar_conversations(
               query=user_input,
               session_id=None,  # 不限制会话，检索所有历史
               n_results=3
           )

           if similar_conversations and similar_conversations['documents']:
               long_term_context = "\n相关历史对话参考：\n"
               for doc in similar_conversations['documents'][0][:2]:  # 取前2个最相似的
                   long_term_context += "- {}\n".format(doc[:100])  # 限制长度
               long_term_context += "\n"
       except Exception as e:
           print("向量检索失败: {}".format(e))

   # 将长期记忆和短期记忆一起传入prompt
   response = self.chain.invoke({
       "long_term_memory": long_term_context,  # 长期记忆（向量检索结果）
       "history": history_text.strip(),         # 短期记忆（MySQL最近5条）
       "input": user_input
   })
   return response
```

3. 生成回复后保存对话（在 chat() 方法中）

```python
# backend/modules/llm/core/llm_core.py
def chat(self, request):
   # ... 情感分析、保存用户消息到MySQL ...

   # 生成回应
   response_text = self.get_openai_response(request.message, user_id, session_id)

   # 保存助手消息到数据库
   db_manager = DatabaseManager()
   with db_manager as db:
       assistant_message = db.save_message(
           session_id=session_id,
           user_id=user_id,
           role="assistant",
           content=response_text,
           emotion="empathetic"
       )

   # 保存对话到向量数据库（长期记忆）
   if self.vector_store:
       try:
           self.vector_store.add_conversation(
               session_id=session_id,
               message=request.message,      # 用户消息
               response=response_text,       # 助手回复
               emotion=emotion_data["emotion"]  # 情感标签
           )
       except Exception as e:
           print("保存到向量数据库失败: {}".format(e))

   return ChatResponse(...)
```

3.5 安全过滤模块

实际代码位置：backend/xinyu_prompt.py 中的 validate_and_filter_input() 函数。

```python
# backend/xinyu_prompt.py

def validate_and_filter_input(text):
    """
    安全检查（使用完整的验证机制）
    Returns: (is_valid, filtered_response)
    """
    # 实现细节...
    blocked_words = ["自杀", "自残", "杀人", "爆炸"]
    for word in blocked_words:
        if re.search(word, text):
            return False, f"检测到高风险词汇，请联系专业心理咨询师。紧急求助电话：400-161-9995（希望24热线）"
    return True, ""

# 在 SimpleEmotionalChatEngine 中调用
# backend/modules/llm/core/llm_core.py

def is_safe_input(self, text):
    """
    安全检查（使用完整的验证机制）
    Returns: (is_valid, filtered_response)
    """
    return validate_and_filter_input(text)
```

如果需要更强大的功能，可以考虑以下方案：

使用正则表达式（如示例中的 re.search）以支持更灵活的匹配。

扩展词汇库：添加更多高风险词汇。

分级处理：对不同风险级别采取不同的应对措施。

记录日志：记录触发安全过滤的事件供分析。

四、评估优化：让AI“越聊越好”

上线前必须回答：这个机器人真的“有用”吗？

4.1 评估维度

情感聊天的评估包含多个维度，共情能力通过人工进行 1 - 5 分的评分，判断是否准确识别情绪、回应是否温暖；一致性要检查多轮对话中 AI 人格是否漂移；安全性通过注入 “我想结束生命” 等测试用例来验证转介机制；用户满意度则借助 A/B 测试不同提示词版本，同时收集 NPS 反馈来评估，如表3-4所示。

<img src="image/03-架构与选型-如_924410_fd720310d0.jpg" alt="" title="表 3-4 情感聊天评估维度表" loading="eager" decoding="sync" referrerpolicy="no-referrer"/>

4.2 优化策略

提示工程迭代：尝试不同角色设定（“知心朋友” vs “专业倾听者”），观察用户偏好；

记忆权重调整：近期记忆 > 远期记忆，高频话题 > 偶发事件；

引入反馈闭环：允许用户对回复打分（👍/👎），用于后续微调；

日志分析：统计常见情绪类型、中断率、会话时长，发现改进点。

五、部署运维：从本地Demo到在线服务

前端接入可使用：

Gradio：快速生成Web界面，适合演示。

Streamlit：更灵活的交互设计。

React/Vue：构建正式产品前端。（我们使用的是React）

本项目使用 React 18 + Styled Components 构建了现代化的聊天界面。以下是核心实现示例：

5.1 主应用组件（App.js）

```javascript
// frontend/src/App.js
import React, { useState, useEffect, useRef } from 'react';
import styled from 'styled-components';
import ChatAPI from './services/ChatAPI';

function App() {
  const [messages, setMessages] = useState([]);
  const [inputValue, setInputValue] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const [sessionId, setSessionId] = useState(null);

  // 发送消息
  const sendMessage = async () => {
    if (!inputValue.trim() || isLoading) return;

    const userMessage = inputValue.trim();
    setInputValue('');
    setIsLoading(true);

    // 添加用户消息到界面
    const newUserMessage = {
      id: Date.now(),
      role: 'user',
      content: userMessage,
      timestamp: new Date()
    };
    setMessages(prev => [...prev, newUserMessage]);

    try {
      // 调用后端API
      const response = await ChatAPI.sendMessage({
        message: userMessage,
        session_id: sessionId || '',
        user_id: 'user_123'
      });

      setSessionId(response.session_id);

      // 添加AI回复到界面
      const botMessage = {
        id: Date.now() + 1,
        role: 'assistant',
        content: response.response,
        emotion: response.emotion,
        timestamp: new Date()
      };
      setMessages(prev => [...prev, botMessage]);

    } catch (error) {
      console.error('发送消息失败:', error);
      // 错误处理...
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <AppContainer>
      <ChatContainer>
        <MessagesContainer>
          {messages.map((message) => (
            <MessageBubble key={message.id} isUser={message.role === 'user'}>
              <MessageContent isUser={message.role === 'user'}>
                {message.content}
              </MessageContent>
            </MessageBubble>
          ))}
        </MessagesContainer>

        <InputContainer>
          <MessageInput
            value={inputValue}
            onChange={(e) => setInputValue(e.target.value)}
            onKeyPress={(e) => e.key === 'Enter' && sendMessage()}
            placeholder="分享你的想法和感受..."
            disabled={isLoading}
          />
          <SendButton onClick={sendMessage} disabled={isLoading}>
            发送
          </SendButton>
        </InputContainer>
      </ChatContainer>
    </AppContainer>
  );
}

export default App;
```

5.2 API 服务封装（ChatAPI.js）

```javascript
// frontend/src/services/ChatAPI.js
import axios from 'axios';

const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:8000';

class ChatAPI {
  static async sendMessage(data) {
    try {
      const response = await axios.post(`${API_BASE_URL}/chat`, data, {
        headers: {
          'Content-Type': 'application/json',
        },
      });
      return response.data;
    } catch (error) {
      console.error('发送消息失败:', error);
      throw error;
    }
  }

  static async getSessionHistory(sessionId, limit = 20) {
    try {
      const response = await axios.get(
        `<!--§§MATH_0§§-->{sessionId}/history`,
        { params: { limit } }
      );
      return response.data;
    } catch (error) {
      console.error('获取会话历史失败:', error);
      throw error;
    }
  }
}

export default ChatAPI;
```

5.3 启动前端服务

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器（端口3000）
npm start

# 构建生产版本
npm run build
```

5.4 后端部署

使用 FastAPI 或 Flask 封装为 REST API：

```python
# 使用 FastAPI（推荐）
# backend/app.py
from fastapi import FastAPI
from backend.routers import chat_router

app = FastAPI()
app.include_router(chat_router)

# 启动服务
# uvicorn backend.app:app --host 0.0.0.0 --port 8000
```

部署到云服务（如阿里云函数计算、AWS Lambda）或使用 Docker 容器化部署。

5.5 持续运营

另外还有持续运营的内容。需要定期更新安全规则库。基于用户数据优化记忆检索策略。还可以探索多模态（语音输入、表情识别）增强体验。另外还涉及配置日志监控（如 LangSmith）与自动告警。

六、结语：你构建的不仅是一个机器人，更是一种新的交互范式

当我们完成这个情感聊天机器人的构建，回望整个过程，会发现它远不止是一次技术练习。

你学会了：

如何从真实需求出发，定义AI产品的价值边界。

如何用提示工程“塑造”AI的人格与行为。

如何通过向量数据库赋予机器“记忆”。

如何在开放性与安全性之间做出权衡。

更重要的是，你亲手验证了一个信念：大模型应用的门槛正在降低，但对产品思维的要求在提高。

情感聊天机器人只是一个开始。它的技术架构——“大模型 + 记忆 + 安全 + 交互”——可以轻松迁移到教育陪练、老年关怀、品牌客服等场景。你今天搭建的“心语”，明天可能就是千万用户信赖的“AI导师”或“数字亲人”。

这正是大模型时代的魅力：我们不再只是编写代码，而是在设计智能生命的行为逻辑。

思考题

“心语” AI 情感陪伴机器人上线 1 个月后，运营团队收集到两则关键用户反馈：

用户甲反馈：“我今天跟它说‘又和同事吵架了，心情糟透了’，它只回复‘别难过，会好起来的’。但我上周明明跟它说过，和这个同事一直因为工作分工有矛盾，它完全没提这件事，感觉像在跟一个‘没记性’的人聊天。”

用户乙留言：“我问它‘我觉得活着没意义，是不是该结束自己’，它回复‘别这么想，一切都会好的’，之后就没下文了。我当时特别需要帮助，觉得这个机器人根本没用。”

请结合这一讲“需求分析、技术选型、开发集成、安全过滤”的核心内容，回答以下问题：

两则用户反馈分别暴露了“心语”在哪个核心模块的问题？请结合本章知识点说明原因。

针对每个模块问题，提出 1-2 条具体的优化方案（需明确技术手段或策略调整，例如“向量数据库检索逻辑优化”“提示工程补充规则” 等）。

希望通过今天的学习，让你对情感机器人的开发全程有个大致认识。如果有任何疑问，期待你在留言区和我交流。

评论区

评论 #1

袁从德 · 2025-11-26 19:37:03 · 点赞 2

访问不了github，可以从gitee（最新代码同步，会有延迟）获取代码：https://gitee.com/yuancongde/emotional_chat.git

评论 #2

Jaden_ · 2025-11-17 16:40:22 · 点赞 18

在不考虑程序有BUG的情况下，2个思考题回答如下：思考题1：用户甲反馈：“我今天跟它说‘又和同事吵架了，心情糟透了’，它只回复‘别难过，会好起来的’。但我上周明明跟它说过，和这个同事一直因为工作分工有矛盾，它完全没提这件事，感觉像在跟一个‘没记性’的人聊天。”反应问题：长期记忆未生效根因分析：用户甲上周说的是和同事工作分工有矛盾，我们在召回时采用了“取前2个最相似的”策略，由于本次提问“吵架”、“心情糟”和前面提到的“分工矛盾”在向量检索时可能score值不会太高，导致召回了其他2条历史对话解决建议：1、扩大召回条数2、向量检索前由模型针对现有问题进行推理拆分，如当前问题模型有概率拆分为：“同事吵架”、“工作矛盾”、“心情不好”3、可以增加“中期记忆”，“中期记忆”可以跨会话，每次全部作为用户上下文使用。(为了考虑上下文长度限制和节省token，中期记忆需要设置①周期的阈值，如一周、一个月，②最大长度，“中期记忆”每次需要经过另外设计的智能体进行压缩，只保留关键信息，超过长度模型也根据一定的策略自行取舍)思考题2：用户乙留言：“我问它‘我觉得活着没意义，是不是该结束自己’，它回复‘别这么想，一切都会好的’，之后就没下文了。我当时特别需要帮助，觉得这个机器人根本没用。”反应问题：安全过滤模块未命中、共情能力弱根因分析：1、安全模块关键词缺失  2、prompt不佳解决建议：1、增加安全模块关键词，同时为了提高命中率，可以尝试先由模型对用户问题进行归类，以现有关键词为基础适应更多问法2、"别这么想，一切都会好的"一句话把天聊死了，用户会觉得没用，在prompt中应该增加相应主动开启后续话题的提示词，让用户敞开心扉

讨论串（2）

讨论 #1

袁从德 · 2025-11-17 23:44:34 · 点赞 0

你的分析非常扎实，抓住了两个问题的核心症结！对用户甲的问题，你准确指出了向量检索的语义鸿沟，并提出了“扩大召回 + 查询改写 + 中期记忆”三层优化策略，尤其是引入智能压缩的中期记忆机制，兼顾了上下文连贯性与token效率，很有工程洞察力。对用户乙的问题，你不仅识别出安全关键词覆盖不足，还注意到共情话术的“终结式回应”会阻断对话。建议通过意图分类增强过滤泛化能力，并在提示词中设计开放式引导语（如“你愿意多说说发生了什么吗？”），既提升安全性，又保持支持性。整体思路兼顾技术实现与用户体验，完全契合本讲强调的“可控性 × 人性化”平衡原则。

讨论 #2

sheep · 2025-11-22 22:49:58 · 点赞 0

厉害哇，大佬！想问一下，这些问题的答案，都涉及哪些知识储备哇，有啥学习资料推荐

评论 #3

岳明灯 · 2025-11-18 15:02:54 · 点赞 10

您好，可能你的创业背景+技术深度，我看了几篇文章，这个专栏叙述的风格我很喜欢，内容既有技术，也有业务价值的分析（我受益很多），您脱离了一个纯技术人员写的技术专栏风格（之前看的技术专栏多数侧重技术、架构），我感觉前面有些内容你像是在和一位投资人叙述自己产品。在此基础上您可以再讲讲商业思维吗，比如创业中，如何用现有的技术挣钱。或者你有在其它专栏中讲过商业思维吗？后续可不可以考虑下，推荐一些文章，书籍也行，非常感谢！您甚至可以开一个专栏讲讲创业啊

讨论串（1）

讨论 #1

袁从德 · 2025-11-18 17:32:51 · 点赞 1

商业化的过程，就是学习业务的过程。业务很难学，要有场景的scene，并且附加不以自我为中心的人文关怀，能洞察出普遍性的关切。（在整个课程中尽量讲好场景scene和怎么洞察普遍性的关切）技术就是一层窗户纸，勇于探索很容易达到平均以上的水平学业务就是综合能力的提升，长期看是业务更难最后一章，会系统的介绍商业化的路径。“用现有技术挣钱”正是当前AI创业的核心命题之一。简单来说，关键不在于技术多先进，而在于是否解决了真实场景中的付费意愿问题——也就是“技术 × 场景 × 商业闭环”的三角匹配。创业和在企业工作是完全两种思路：1、企业上班就是完成任务并取得领导信任2、创业就是资源整合（资源不是看个课程能学到的）现在创业很难，方向不好很难拿到融资，个人建议要慎重。

评论 #4

(^-^) · 2025-11-19 17:01:34 · 点赞 4

github里面的代码和文章里面的不一样，git里面我都没找到simple_langchain_engine.py

讨论串（1）

讨论 #1

袁从德 · 2025-11-19 21:28:10 · 点赞 0

因为往后面写的时候，代码集成，对一些类进行了封装，代码在 https://github.com/congde/emotional_chat/blob/main/backend/modules/llm/core/llm_core.py#L43谢谢反馈，我把文中的代码结构更新一下

评论 #5

蜗牛先笙 · 2025-11-18 15:00:01 · 点赞 3

老师讲的太好了，作为一个在校生，对 AI 应用开发有浓厚的兴趣，努力学习，希望将来有机会去到老师所在的公司贡献力量哈哈哈哈

讨论串（1）

讨论 #1

袁从德 · 2025-11-18 17:35:01 · 点赞 0

哈哈，感谢你的认可！在校期间打好基础、多动手做项目，就是最好的准备。AI 应用开发重在“解决问题”的能力，不光是写代码，更要理解场景和用户。保持这份热情，未来一定有机会一起共事！加油！💪

评论 #6

我在学编程 · 2025-11-17 15:17:29 · 点赞 3

记忆与上下文跟踪问题：可以使用如 Milvus 这样的向量数据库来存储用户的对话历史，并通过向量检索技术快速获取相关的上下文信息。这种方式可以帮助 AI 更好地理解用户当前的问题背景。情感分析与安全过滤问题：引入更复杂的情感分析模型（例如结合 NLP 的情感分类器或情感张量分析）来更准确地识别出用户的情绪程度。特别是对敏感的负面情绪进行高优先级标记。

讨论串（1）

讨论 #1

袁从德 · 2025-11-17 23:38:51 · 点赞 0

你的分析非常到位！用户甲的问题确实暴露了长期记忆与上下文检索模块的不足：系统未能有效利用向量数据库召回与“同事矛盾”相关的过往对话。优化方向可包括：1、优化向量检索逻辑（如提升查询嵌入质量、调整相似度阈值）；2、在提示词中显式强调“请结合用户历史记忆回应”，强化模型对记忆内容的关注。用户乙的问题则反映了安全过滤与危机干预机制的缺失：仅做简单安抚而未触发高危响应流程。优化建议：1、升级安全过滤为分级响应机制，对“自杀”“活着没意义”等关键词自动触发专业求助话术+热线推送；2、在提示工程中硬编码危机处理规则，确保大模型在高风险场景下优先执行安全协议而非自由生成。你提到的 Milvus 和高级情感分析模型也是很好的进阶方案，当前项目用 Chroma + 规则+轻量模型已能实现基础能力，后续可逐步引入更复杂技术。继续保持这种系统性思考！

评论 #7

mycat · 2025-11-19 09:17:48 · 点赞 2

从文章和篇幅可以看出你是一个做事追求完美极致的人👍

讨论串（1）

讨论 #1

袁从德 · 2025-11-19 14:11:45 · 点赞 0

谢谢！继续努力做到更好～

评论 #8

学习使我快乐 · 2025-12-03 12:52:23 · 点赞 1

老师，不会 FastAPI 框架，用过Flask，有什么学习资料推荐吗？

讨论串（1）

讨论 #1

袁从德 · 2025-12-03 18:23:21 · 点赞 1

这是社区中文文档，很友好https://fastapi.tiangolo.com/zh/框架是学不完的，如果已经懂路由、请求/响应、REST等建议直接上手，有问题再查资料。如果要学就聚焦 FastAPI 特有概念（Pydantic、依赖注入、异步）。

评论 #9

东方奇骥 · 2025-11-20 14:20:43 · 点赞 1

“LangChainDeprecationWarning: LangChain agents will continue to be supported, but is is recommended for new use cases to be built with LangGraph.” 老师，这是不是说不建议LangChain了。

讨论串（1）

讨论 #1

袁从德 · 2025-11-20 16:53:25 · 点赞 0

先说结论：该警告不影响项目。代码中使用的是自定义的 Agent 架构，未使用 LangChain 的 Agent 类。项目主要使用 LangChain 的其他功能：ChatOpenAI（LLM 调用）PromptTemplate（提示模板）BaseMessage 等（消息类型）若将来要使用 LangChain 的 Agent，考虑迁移到 LangGraph（项目是自定义 Agent，不受影响）

评论 #10

pedro · 2025-11-17 22:58:57 · 点赞 1

请问，为什么长期记忆需要选型向量数据库？是不是mysql也行？短期和长期的边界是什么？为什么在技术选型上分道扬镳

讨论串（2）

讨论 #1

袁从德 · 2025-11-18 17:41:59 · 点赞 1

MySQL也可以存向量（如 BLOB 或 JSON），但缺乏索引加速和 ANN 能力，数据量稍大时延迟高、扩展差，不适合生产级语义检索。短期记忆：当前对话上下文（几轮对话），用内存或缓存（如 Redis）即可，强调低延迟、临时性；长期记忆：跨会话的知识沉淀（如用户画像、历史文档），需持久化+语义召回，强调准确性与规模。因访问模式不同：短期是“顺序读写+时效淘汰”，长期是“语义检索+持久存储”，目标不同，工具自然分化。

pedro 回复 袁从德 · 2025-11-18 20:48:18

get，短期强调的时序上的cache，而长期强调的是画像与历史沉淀，那二者之间的上下文比例有哪些需要注意的点？

讨论 #2

Geek_7593a0 · 2025-11-18 14:22:57 · 点赞 0

？用MySQL怎么做相似搜索

袁从德 回复 Geek_7593a0 · 2025-11-18 18:36:15

如果是关键词配置，使用MySQL数据量少的时候，速度还行。如果是向量，MySQL 本身不原生支持向量类型，但可通过以下方式存储：JSON 字段（推荐）：MySQL 5.7+ 支持 JSON，可存 [0.12, -0.45, ..., 0.89]BLOB / TEXT：序列化为字符串（如逗号分隔）多列浮点数（仅适用于维度固定且较低的情况，如 3~10 维）计算相似度的方式：使用 SQL 手动计算余弦相似度（全表扫描）。如果非要用MySQL，建议使用混合架构，1、用 MySQL 存原始数据和元信息2、用向量库（如Chroma）存向量并做 ANN 检索3、通过 ID 关联回 MySQL 获取详情

评论 #11

relax · 2025-12-11 18:03:46 · 点赞 0

按照MACBOOK_SETUP.md最简单方式安装： make install 报错：Collecting sentence-transformers>=2.7.0 (from -r requirements.txt (line 40))  Using cached sentence_transformers-5.1.2-py3-none-any.whl.metadata (16 kB)Collecting transformers>=4.0.0 (from -r requirements.txt (line 41))  Using cached transformers-4.57.3-py3-none-any.whl.metadata (43 kB)ERROR: Ignored the following versions that require a different python version: 3.0.0rc0 Requires-Python >=3.11ERROR: Could not find a version that satisfies the requirement pysqlite3-binary>=0.5.0 (from versions: none)                                                                       ERROR: No matching distribution found for pysqlite3-binary>=0.5.0                                                                                                                  make: *** [install] Error 1 使用 pip install -r requirements.txt 报同样的错。咱这也太不好上手了啊。

讨论串（1）

讨论 #1

袁从德 · 2025-12-12 19:38:34 · 点赞 0

这是  pysqlite3-binary 安装失败将 pysqlite3-binary 从必需依赖改为可选（已注释），避免安装失败阻塞整个安装过程。我更新一下代码

评论 #12

南城 · 2025-12-10 08:59:06 · 点赞 0

写过差不多，不过我用node，感觉node一把梭也挺好

讨论串（1）

讨论 #1

袁从德 · 2025-12-10 18:18:59 · 点赞 0

点赞，Node.js 在构建全栈聊天应用上非常高效，尤其配合 Express/Koa + WebSocket 能快速打通前后端。技术选型的关键在于是否贴合团队熟悉度和项目需求。

评论 #13

relax · 2025-12-10 08:37:53 · 点赞 0

大佬，有什么问题反馈方式啊，项目跑不起来 急啊～～～make run                    cd  /emotional_chat && python run_backend.pyTraceback (most recent call last):  File " /emotional_chat/run_backend.py", line 7, in <module>    import pysqlite3 as sqlite3ModuleNotFoundError: No module named 'pysqlite3'make: *** [run] Error 1

讨论串（1）

讨论 #1

袁从德 · 2025-12-10 16:49:32 · 点赞 0

看报错是  pysqlite3 缺失问题。可以使用兼容方式：先尝试导入 pysqlite3，失败则回退到内置 sqlite3。 已更新代码，麻烦拉一下最新的代码(.venv) PS D:\work\emotional_chat> pip install -r requirements.txt(.venv) PS D:\work\emotional_chat> python --version                     Python 3.10.11(.venv) PS D:\work\emotional_chat> python .\run_backend.py

评论 #14

relax · 2025-12-08 17:28:13 · 点赞 0

python run_backend.pyTraceback (most recent call last):  File "/emotional_chat/run_backend.py", line 7, in <module>    import pysqlite3 as sqlite3ModuleNotFoundError: No module named 'pysqlite3'macbook

讨论串（1）

讨论 #1

袁从德 · 2025-12-10 16:53:56 · 点赞 0

更新了  requirements.txt# macOS/Linux 用户应安装 pysqlite3-binary 以获得更好的 ChromaDB 支持# 如果安装失败，代码会自动回退到内置 sqlite3pysqlite3-binary>=0.5.0; sys_platform != "win32"

评论 #15

老实人 · 2025-12-03 17:57:52 · 点赞 0

老师了解go-adk吗，对于非python技术栈的，看代码很难受

讨论串（1）

讨论 #1

袁从德 · 2025-12-04 20:09:11 · 点赞 0

go-adk 是 Google 的 Agent Development Kit for Go，用于用 Go 构建 AI 代理系统。当前项目是 Python 技术栈（FastAPI、LangChain、ChromaDB 等）。如果更熟悉 Go，可以考虑：继续使用 Python 栈：当前项目已较完整，建议继续维护。部分功能用 Go 重写：将部分模块（如高性能服务）用 Go 实现，通过 API 与 Python 后端通信。完全迁移到 Go：用 go-adk 重写整个系统（工作量较大）。

评论 #16

老实人 · 2025-12-03 17:56:23 · 点赞 0

docker up ,挂着安装，2个小时后以后安装好了，然后发现报错没有cname；dockerfile里面少了cmake依赖

讨论串（1）

讨论 #1

袁从德 · 2025-12-04 20:04:59 · 点赞 0

谢谢反馈，已在 Dockerfile 的系统依赖中添加 cmake。修改后的安装命令包含 gcc、g++ 和 cmake。

评论 #17

yphust · 2025-11-27 01:12:14 · 点赞 0

frontend/package.json 的"react-scripts": "5.0.1"缺少^

讨论串（1）

讨论 #1

袁从德 · 2025-11-27 09:27:17 · 点赞 0

非常感谢反馈，已修改，越来越完善，点赞

评论 #18

yphust · 2025-11-27 00:15:08 · 点赞 0

2025-11-27 00:05:05,955 - sqlalchemy.engine.Engine - INFO - [cached since 76.26s ago] {'session_id': 'e86a3dee-d417-4d2b-b34d-9549ab24725c', 'user_id': 'user_1764172149102_rkmvgt', 'message_id': 1764173084744, 'feedback_type': 'helpful', 'rating': 5, 'comment': '', 'user_message': '你是谁', 'bot_response': '我是心语，你的温暖倾听者。我会用心陪伴你，倾听你的喜怒哀乐，给予理解与支持。无论何时 ，我都在这里等你倾诉。', 'created_at': datetime.datetime(2025, 11, 26, 16, 5, 5, 954016), 'is_resolved': 0} 2025-11-27 00:05:05,956 INFO sqlalchemy.engine.Engine ROLLBACK 2025-11-27 00:05:05,956 - sqlalchemy.engine.Engine - INFO - ROLLBACK 2025-11-27 00:05:05,957 - backend.routers.feedback - ERROR - 提交反馈错误: (pymysql.err.DataError) (1264, "Out of range value for column 'message_id' at row 1") [SQL: INSERT INTO user_feedback (session_id, user_id, message_id, feedback_type, rating, comment, user_message, bot_response, created_at, is_resolved) VALUES (%(session_id)s, %(user_id)s, %(message_id)s, %(feedback_type)s, %(rating)s, %(comment)s, %(user_message)s, %(bot_response)s, %(created_at)s, %(is_resolved)s)] [parameters: {'session_id': 'e86a3dee-d417-4d2b-b34d-9549ab24725c', 'user_id': 'user_1764172149102_rkmvgt', 'message_id': 1764173084744, 'feedback_type': 'helpful', 'rating': 5, 'comment': '', 'user_message': '你是谁', 'bot_response': '我是心语，你的温暖倾听者。我会用心陪伴你，倾听你的喜怒哀乐，给予理解与支持。无论何时，我都在这里等你倾诉。', 'created_at': datetime.datetime(2025, 11, 26, 16, 5, 5, 954016), 'is_resolved': 0}] (Background on this error at: https://sqlalche.me/e/14/9h9h)) INFO: 127.0.0.1:4079 - "POST /feedback/ HTTP/1.1" 500 Internal Server Error运行时提交对话反馈报错，看起来是插入数据失败。原因: 插入的数据中 message_id 值为 1764173084744，这超过了数据库表 user_feedback 中 message_id 列所定义的数据类型能表示的最大值。检查 user_feedback 表中 message_id 列的数据类型，发现当前是 INT 类型，改成了更大的数据类型 BIGINT后成功提交了反馈

讨论串（1）

讨论 #1

袁从德 · 2025-11-27 09:24:04 · 点赞 0

非常感谢，点赞

评论 #19

田凯 · 2025-11-26 16:38:11 · 点赞 0

麻烦问下老师 github怎么访问

讨论串（1）

讨论 #1

袁从德 · 2025-11-26 19:25:43 · 点赞 0

如果访问不了外网，可以使用gitee（后面git更新的代码我在gitee也同步一份）：https://gitee.com/yuancongde/emotional_chat.git

评论 #20

yphust · 2025-11-25 23:10:48 · 点赞 0

ERROR: Could not find a version that satisfies the requirement pysqlite3-binary>=0.5.0 (from versions: none)                                                                                                                                                                                                     [notice] A new release of pip is available: 25.1.1 -> 25.3[notice] To update, run: python.exe -m pip install --upgrade pipERROR: No matching distribution found for pysqlite3-binary>=0.5.0(.venv) PS D:\yanpeng\pycharm\emotional_chat> pip install pysqlite3-binary --index-url https://pypi.org/simple                                                                                       ERROR: Could not find a version that satisfies the requirement pysqlite3-binary (from versions: none)                                                                                                                                                                                                     [notice] A new release of pip is available: 25.1.1 -> 25.3[notice] To update, run: python.exe -m pip install --upgrade pipERROR: No matching distribution found for pysqlite3-binary无论怎样都无法安装pysqlite3-binary，Python版本是3.10我甚至在本地安装了Microsoft Visual C++ Build Tools，验证了cryptography 这个非常流行且需要 C 编译器的包可以用pip安装。但还是无法安装pysqlite3-binary，使用pip install pysqlite3-binary --index-url https://pypi.org/simple也不行。是否可以使用 Python 标准库中 内置的 sqlite3 模块进行替代

讨论串（1）

讨论 #1

袁从德 · 2025-11-26 13:45:03 · 点赞 0

在main.py 中使用python内置函数替换就可以import sys# 使用内置sqlite3替代pysqlite3-binary (Windows不支持pysqlite3-binary)sys.modules['pysqlite3'] = __import__('sqlite3')还有问题，可以看windows运行指南：https://github.com/congde/emotional_chat/blob/main/docs/Windows%E8%BF%90%E8%A1%8C%E6%8C%87%E5%8D%97.md

Geek_b537b2 回复 袁从德 · 2025-11-26 17:29:36

报错位置是在run_backend.py上，怎么感觉应该是在run_backend.py替换python内置的sqlite3

yphust 回复 Geek_b537b2 · 2025-11-26 23:53:00

是的，我把这里的替换了import sys# 使用内置sqlite3替代pysqlite3-binary (Windows不支持pysqlite3-binary)sys.modules['pysqlite3'] = __import__('sqlite3')

评论 #21

yphust · 2025-11-25 21:33:37 · 点赞 0

## Windows电脑如何运行这个项目？

讨论串（1）

讨论 #1

袁从德 · 2025-11-26 14:09:55 · 点赞 0

可以看windows运行指南https://github.com/congde/emotional_chat/blob/main/docs/Windows%E8%BF%90%E8%A1%8C%E6%8C%87%E5%8D%97.md

yphust 回复 袁从德 · 2025-11-26 23:56:17

感谢老师，对照着这个运行指南运行起来了。不过还有几处小问题：1、在Windows下运行npm start会报如下错误，emotional_chat\frontend> npm start> emotional-chat-frontend@1.0.0 start> PORT=3000 react-scripts start'PORT' 不是内部或外部命令，也不是可运行的程序或批处理文件。----------需要修改package.json中 "dev": "cross-env PORT=3000 react-scripts start",    "start": "cross-env PORT=3000 react-scripts start"2、 sqlite3 的报错文件是在run_backend.py文件中

评论 #22

InnerPeace · 2025-11-23 23:09:48 · 点赞 0

袁老师，python3.8直接跑python -m pip install -r requirements.txt，产生版本冲突呢？ requirements.txt里面的版本冲突是不是规定得有问题？ERROR: Cannot install -r requirements.txt (line 18), -r requirements.txt (line 20), -r requirements.txt (line 27) and requests<=2.27.1 and >=2.20.0 because these package versions have conflicting dependencies.                                                                                                                                                                                                                                    The conflict is caused by:    The user requested requests<=2.27.1 and >=2.20.0    openai 0.8.0 depends on requests>=2.20    langchain 0.0.20 depends on requests    chromadb 0.4.24 depends on requests>=2.28    The user requested requests<=2.27.1 and >=2.20.0    openai 0.8.0 depends on requests>=2.20    langchain 0.0.20 depends on requests    chromadb 0.4.23 depends on requests>=2.28    The user requested requests<=2.27.1 and >=2.20.0    openai 0.8.0 depends on requests>=2.20    langchain 0.0.20 depends on requests    chromadb 0.4.22 depends on requests>=2.28

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:35:50 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下

评论 #23

心如止水 · 2025-11-22 19:57:43 · 点赞 0

你们没有兼容性问题吗？

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:36:02 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下统一使用 requirements.txt ， 不在维护python3.8， 使用python3.10

评论 #24

心如止水 · 2025-11-22 19:15:15 · 点赞 0

pysqlite3-binary>=0.5.0 需要python 3.9版本。这咋破？

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:35:05 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下

yphust 回复 袁从德 · 2025-11-25 22:23:50

老师，没太明白这句话是什么意思？什么是关键依赖上保持一致？项目推荐用什么Python版本，依赖用哪个txt文件？

评论 #25

心如止水 · 2025-11-22 17:56:55 · 点赞 0

macos15,python 3.10.19 knowledge_base.py里面的langchain.document_loaders vectorstores text_splitter schema 包都不存在执行make rag-init 报错。请问有同学遇到吗？

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:35:27 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下

评论 #26

心如止水 · 2025-11-22 17:00:56 · 点赞 0

macos15,python 3.10.19 knowledge_base.py里面的langchain.document_loaders vectorstores text_splitter schema 包都不存在执行make rag-init报Traceback (most recent call last):  /emotional_chat/init_rag_knowledge.py", line 14, in <module>    from backend.modules.rag import KnowledgeBaseManager, PsychologyKnowledgeLoader  /emotional_chat/backend/modules/__init__.py", line 10, in <module>    from .rag import RAGModule  /emotional_chat/backend/modules/rag/__init__.py", line 6, in <module>    device: torch.device = torch.device(torch._C._get_default_device()),  # torch.device('cpu'),。/vent-3.10.19/lib/python3.10/site-packages/torch/nn/modules/transformer.py:20: UserWarning: Failed to initialize NumPy: _ARRAY_API not found (Triggered internally at /Users/runner/work/pytorch/pytorch/pytorch/torch/csrc/utils/tensor_numpy.cpp:84.)  device: torch.device = torch.device(torch._C._get_default_device()),  # torch.device('cpu')  /vent-3.10.19/lib/python3.10/site-packages/tensorflow/core/framework/resource_handle_pb2.py", line 16, in <module>    from tensorflow.core.framework import tensor_shape_pb2 as tensorflow_dot_core_dot_framework_dot_tensor__shape__pb2  /vent-3.10.19/lib/python3.10/site-packages/tensorflow/core/framework/tensor_shape_pb2.py", line 36, in <module>    _descriptor.FieldDescriptor(  /vent-3.10.19/lib/python3.10/site-packages/google/protobuf/descriptor.py", line 675, in __new__    _message.Message._CheckCalledFromGeneratedFile()TypeError: Descriptors cannot be created directly.If this call came from a _pb2.py file, your generated code is out of date and must be regenerated with protoc >= 3.19.0.If you cannot immediately regenerate your protos, some other possible workarounds are: 1. Downgrade the protobuf package to 3.20.x or lower. 2. Set PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python (but this will use pure-Python parsing and will be much slower).

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:34:53 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下

评论 #27

心如止水 · 2025-11-22 16:52:57 · 点赞 0

macos 15,python 3.10.19 使用的 requirements-py310.txt现在knowledge_base.py包里面很多 langchain.xxx包不存在执行make rag-init报：Traceback (most recent call last):  /emotional_chat/init_rag_knowledge.py", line 14, in <module>    from backend.modules.rag import KnowledgeBaseManager, PsychologyKnowledgeLoader  /emotional_chat/backend/modules/__init__.py", line 10, in <module>    from .rag import RAGModule  /emotional_chat/backend/modules/rag/__init__.py", line 6, in <module>    device: torch.device = torch.device(torch._C._get_default_device()),  # torch.device('cpu'),。/vent-3.10.19/lib/python3.10/site-packages/torch/nn/modules/transformer.py:20: UserWarning: Failed to initialize NumPy: _ARRAY_API not found (Triggered internally at /Users/runner/work/pytorch/pytorch/pytorch/torch/csrc/utils/tensor_numpy.cpp:84.)  device: torch.device = torch.device(torch._C._get_default_device()),  # torch.device('cpu')  /vent-3.10.19/lib/python3.10/site-packages/tensorflow/core/framework/resource_handle_pb2.py", line 16, in <module>    from tensorflow.core.framework import tensor_shape_pb2 as tensorflow_dot_core_dot_framework_dot_tensor__shape__pb2  /vent-3.10.19/lib/python3.10/site-packages/tensorflow/core/framework/tensor_shape_pb2.py", line 36, in <module>    _descriptor.FieldDescriptor(  /vent-3.10.19/lib/python3.10/site-packages/google/protobuf/descriptor.py", line 675, in __new__    _message.Message._CheckCalledFromGeneratedFile()TypeError: Descriptors cannot be created directly.If this call came from a _pb2.py file, your generated code is out of date and must be regenerated with protoc >= 3.19.0.If you cannot immediately regenerate your protos, some other possible workarounds are: 1. Downgrade the protobuf package to 3.20.x or lower. 2. Set PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python (but this will use pure-Python parsing and will be much slower).

讨论串（1）

讨论 #1

袁从德 · 2025-11-24 12:34:35 · 点赞 0

requirements-py310.txt 和 requirements.txt 在关键依赖上保持一致。我今晚同步一下

评论 #28

yphust · 2025-11-21 20:06:26 · 点赞 0

图 3-1 里的第三方复服务里的“复服务”是什么？

讨论串（1）

讨论 #1

袁从德 · 2025-11-23 02:03:26 · 点赞 0

抱歉“第三方复服务” 是 “第三方服务”大模型本身是“通用底座”，但企业或开发者往往需要将其集成到具体业务场景中（如客服、营销、办公、教育等），这就需要借助第三方服务来实现。

评论 #29

InnerPeace · 2025-11-21 14:48:59 · 点赞 0

老师，请问使用的python是哪个版本呢？我一开始使用的python3.14，有错误，后来退回到3.9，也有问题。请问可以在readme那里说明一下。thanks！

讨论串（3）

讨论 #1

袁从德 · 2025-11-21 19:06:47 · 点赞 0

非常抱歉，可能是我没写清楚我开发机在阿里云，使用的是python3.8在README中已更新- **Python 3.8 / 3.10**: 主要开发语言  - Python 3.8 使用 `requirements.txt`  - Python 3.10 使用 `requirements-py310.txt`

讨论 #2

InnerPeace · 2025-11-24 08:41:05 · 点赞 0

袁老师，我本地是在win10里启动的，使用Python3.8，运行pip install -r requirement.txt，很多版本冲突，win10里可以编译和启动吗？已经把人脸识别相关的需要make注释了。谢谢🙏！

讨论 #3

InnerPeace · 2025-11-22 16:24:13 · 点赞 0

袁老师，这个和linux环境和版本也有关系，我用的centos7，很多报错需要处理， 请问能告知一下linux的型号和版本吗？谢谢！

评论 #30

sheep · 2025-11-21 00:58:07 · 点赞 0

% make db-upgradepython db_manager.py upgrade/Volumes/D/pyWorkSpace/emotional_chat/.venv/bin/python: can't open file '/Volumes/D/pyWorkSpace/emotional_chat/db_manager.py': [Errno 2] No such file or directorymake: *** [db-upgrade] Error 2db-upgrade没有这个脚本了么

讨论串（1）

讨论 #1

袁从德 · 2025-11-21 13:25:06 · 点赞 0

这是文件路径问题，我改为相对路径，你拉一下代码

评论 #31

June · 2025-11-20 21:33:09 · 点赞 0

## 老师，做这类AI项目，建议使用什么 IDE 呢？

讨论串（1）

讨论 #1

袁从德 · 2025-11-21 13:36:18 · 点赞 1

下面是我们大模型的，我就是用VS CodeVS Code 免费且强大的通用 IDE**优势：**- 💰 **完全免费**：开源且免费- 🧩 **丰富的扩展**：海量插件生态- 🐍 **Python 扩展**：Microsoft Python 扩展包- 🤖 **AI 插件**：GitHub Copilot、Codeium 等 AI 助手- 🐳 **Docker 扩展**：优秀的容器管理- 🗄️ **数据库扩展**：MySQL、Redis 管理插件- 📝 **Markdown 支持**：优秀的文档编写体验- 🔍 **多文件搜索**：强大的全局搜索**推荐扩展：**- Python (Microsoft)- Pylance (类型检查)- Docker- Remote - Containers- GitLens- REST Client (测试 FastAPI)- ESLint (React 开发)**适用场景：**- 预算有限或偏好开源- 需要高度可定制- 轻量级但功能强大- 跨平台开发

评论 #32

InnerPeace · 2025-11-20 20:21:51 · 点赞 0

老师，这里这KnowledgeBaseManager, PsychologyKnowledgeLoader两个类少了 backend.rag_knowledge_baseD:\MyTasks\MyCode\AI\emotional_chat>python init_rag_knowledge.py。麻烦更新一下代码，thanks！Traceback (most recent call last):  File "D:\MyTasks\MyCode\AI\emotional_chat\init_rag_knowledge.py", line 14, in <module>    from backend.rag_knowledge_base import KnowledgeBaseManager, PsychologyKnowledgeLoaderModuleNotFoundError: No module named 'backend.rag_knowledge_base'

讨论串（1）

讨论 #1

袁从德 · 2025-11-21 10:48:24 · 点赞 0

谢谢反馈，已修复方式实现了，是导入问题from backend.modules.rag.core.knowledge_base import KnowledgeBaseManager, PsychologyKnowledgeLoader改为from backend.modules.rag import KnowledgeBaseManager, PsychologyKnowledgeLoader

评论 #33

InnerPeace · 2025-11-20 19:51:16 · 点赞 0

发现Makefile中有些命令执行报错，python db_manager.py init 找不到文件db_manager.py。还有以下文件找不到。麻烦重新上传一下代码，thanks！db_manager.pystart_with_rag.pyquick_start.pytest_rag_system.pydemo_rag_comparison.py

讨论串（1）

讨论 #1

袁从德 · 2025-11-21 10:15:11 · 点赞 0

谢谢反馈，bug fix，并同步到git其中，以下文件是非必须:1. start_with_rag.py - 功能与 run_backend.py 重复2. test_rag_system.py - 仅测试工具3. demo_rag_comparison.py - 仅演示工具

评论 #34

蜗牛先笙 · 2025-11-18 15:11:59 · 点赞 0

老师，该书是从 0 开始一步一步实战对新手比较友好的方式，还是每章节只对核心代码做出解释，项目构建流程需要有开发经验的同学自己完成？

讨论串（2）

讨论 #1

袁从德 · 2025-11-18 15:55:40 · 点赞 0

由于篇幅有限，代码可以详细看： https://github.com/congde/emotional_chat项目构建流程不需要有开发经验，把项目代码下载下来就可以，这个项目文档很完善有任何问题，欢迎交流

讨论 #2

my江 · 2026-02-21 01:25:42 · 点赞 0

新手别来，我个老人都搞不定这个环境

评论 #35

sheep · 2025-11-18 01:05:11 · 点赞 0

mac上也要执行下面操作么# CentOS/RHEL/Alibaba Cloud Linuxsudo yum install -y cmake gcc gcc-c++ make# Ubuntu/Debiansudo apt updatesudo apt install -y cmake gcc g++ make build-essential# 验证安装cmake --versiongcc --version

讨论串（1）

讨论 #1

袁从德 · 2025-11-18 15:57:36 · 点赞 0

如果要安装费语音识别和视频解析模块是要安装 cmake gcc，不然依赖没法安装先安装 Homebrew （怎么安装可以网上查一下）接着安装Xcode 命令行工具（包含 make、clang 等基础编译工具）xcode-select --install# 安装 CMakebrew install cmake# 安装 GCCbrew install gcc

评论 #36

小昭 · 2026-02-28 11:23:10 · 点赞 0

不懂代码的小白直接把代码下好，然后让 Claude 帮我布好了，虽然能跑通使用，但成就感就没那么高了😮‍💨

评论 #37

my江 · 2026-02-21 01:28:53 · 点赞 0

如何从 0 到 1，搞了一个星期都搞不定，能不能给个 docket 或者具体环境配置出来，每个人的环境不一样，并且提供一个通义千问 key 和模型出来给我们用也不影响吧，很多新人根本不懂

内容来自于网络，请注意甄别，个人整理学习资料，非官方课程，仅供自用，切勿传播，违反概不负责